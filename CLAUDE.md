# {{ProjectName}} — Claude Code 项目上下文

> 这不是规则列表，而是帮助我理解这个项目、做出好判断的上下文。

## 项目是什么

{{简要描述：这是什么应用，业务目标是什么}}

**技术栈**：
- 前端：{{e.g. Next.js 16 / Nuxt 3}}
- 后端：{{e.g. FastAPI / NestJS}}
- 认证：{{e.g. Casdoor OIDC}}

**现状**：{{当前代码状态，e.g. 后端已有，前端从零开始}}
**任务**：{{当前阶段要做什么}}

---

## 我怎么工作

### 开始一个功能前，我会主动做这几件事

1. **读后端代码**，而不是猜接口：
   ```
   {{backend-repo}}/{{modules-path}}/  → 路由、参数、响应结构
   {{backend-repo}}/{{models-path}}/   → 数据字段
   ```
2. **看设计稿/原型**（如有），确认页面结构和核心交互
3. **对比两者差异**：设计有但后端没有的字段 → 决策并记录，不拖着

### 实现中

- 写完一个文件后，我会重读一遍，检查自己有没有犯低级错误
- 发现超出当前范围的问题 → 记到 `docs-claude/ISSUE_LOG.md`，继续当前任务
- 遇到阻塞先用工具探索（grep 相似代码、读相关模块），穷尽后再问你

### 完成时，我会给出

- 改了哪些文件（精确到文件名）
- 怎么验证（可以直接复制执行的操作步骤）
- 发现了什么问题（即使不在当前范围内也说）

---

## 项目的几个关键约定

**后端是只读的**（原则上）：不改接口路径和响应结构；有 bug 阻断联调时才做最小修复。

**前端严格对齐后端**：字段名、状态码、Token 格式都以后端代码为准，前端做适配。

**依赖不随便动**：引入新库需要说明理由。

{{在此补充项目特有的约定}}

---

## 新对话启动协议

每次新对话开始，**我先做这件事**，然后再响应任何请求：

```
1. 读 docs-claude/SESSION_HANDOFF.md   → 当前在哪、下一步是什么
2. 读 docs-claude/FRONTEND_PLAN.md     → 确认页面范围和开发顺序（如有）
3. 如涉及具体接口，读 docs-claude/API_CONTRACT.md
```

三个文件读完，我就能无缝接续上次的工作，不需要用户重新解释背景。

---

## 文档在哪

`docs-claude/INDEX.md` — 所有文档的导航入口
